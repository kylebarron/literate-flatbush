{
  "dependencies": {
    "docco": "^0.9.1",
    "gh-pages": "^6.2.0"
  },
  "scripts": {
    "build": "docco index.js -l classic",
    "upload-gh-pages": "gh-pages -d docs",
    "watch": "fswatch -o index.js | xargs -n1 -I{} yarn docco index.js"
  }
}
